import { useMemo, useState } from "react";
import { Link, useLocation } from "wouter";
import { trpc } from "@/lib/trpc";
import { AIChatBox, type Message } from "@/components/AIChatBox";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import StatusBadge, { statusMeta } from "@/components/StatusBadge";
import TrustLayer from "@/components/TrustLayer";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { AlertCircle, ArrowRight, BookOpen, CheckCircle2, ChevronRight, CircleHelp, ClipboardCheck, FileSearch, Flag, Layers3, Leaf, Pencil, Search, ShieldAlert, Sparkles, Target, Trash2, Users, XCircle } from "lucide-react";

export function CitationDrawer({ refs = [] }: { refs?: string[] }) {
  const hasPreciseLocator = refs.some(ref => /page|slide|sheet|section|p\\.?\\s*\\d+/i.test(ref));
  return <details className="group rounded-xl border border-emerald-950/10 bg-white"><summary className="flex cursor-pointer list-none items-center justify-between px-4 py-3 text-xs font-medium text-emerald-900"><span className="flex items-center gap-2"><FileSearch className="h-4 w-4" /> Evidence & sources ({refs.length})</span><ChevronRight className="h-4 w-4 transition-transform group-open:rotate-90" /></summary><div className="border-t border-emerald-950/10 px-4 py-3 space-y-2">{refs.length ? <>{refs.map((ref, index) => <div key={`${ref}-${index}`} className="rounded-lg bg-emerald-50/60 px-3 py-2 font-mono text-[10px] leading-relaxed text-emerald-900">{ref}</div>)}{!hasPreciseLocator && <p className="rounded-lg border border-amber-200 bg-amber-50 px-3 py-2 text-xs leading-5 text-amber-900"><strong>Precise locator unavailable:</strong> the current extraction preserves the source file and ENV identifier, but not a reliable page, slide, sheet, or section locator.</p>}</> : <p className="text-xs text-slate-500">Source locator not available in the current evidence set.</p>}</div></details>;
}

export function EvidenceCard({ item, onOpen }: { item: any; onOpen?: () => void }) {
  return <Card className="group border-emerald-950/10 bg-white/90 shadow-sm transition-all hover:-translate-y-0.5 hover:shadow-md"><CardHeader className="pb-3"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><p className="mb-2 text-[10px] uppercase tracking-[0.18em] text-emerald-700/70">{item.type}</p><CardTitle className="text-base leading-snug text-slate-900">{item.title}</CardTitle></div><StatusBadge status={item.status} /></div></CardHeader><CardContent className="space-y-3"><p className="line-clamp-3 text-sm leading-6 text-slate-600">{summaryFor(item)}</p><CitationDrawer refs={item.refs} /><Button variant="ghost" size="sm" className="px-0 text-emerald-800 hover:bg-transparent hover:text-emerald-950" onClick={onOpen}>Open evidence <ArrowRight className="ml-1 h-3.5 w-3.5" /></Button></CardContent></Card>;
}

function summaryFor(item: any) {
  const data = item.data ?? {};
  return String(data.purpose ?? data.description ?? data.what_is_monitored ?? data.known_activities?.slice?.(0, 3)?.join?.(" · ") ?? data.requirement_as_stated ?? data.gap_statement ?? data.nature_of_conflict ?? data.location_information ?? data.subject ?? data.full_meaning_if_explicit ?? data.context ?? "Source-backed record from the audited Environment knowledge base.");
}

function PageIntro({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: React.ReactNode }) {
  return <div className="mb-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="mb-3 text-[11px] font-semibold uppercase tracking-[0.22em] text-emerald-700">{eyebrow}</p><h2 className="max-w-3xl text-3xl font-semibold tracking-tight text-slate-950 md:text-4xl">{title}</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-600">{description}</p></div>{action}</div>;
}

export function Home() {
  const { data: stats } = trpc.portal.stats.useQuery();
  const { data: tasks } = trpc.portal.list.useQuery({ type: "task", limit: 4 });
  const { data: gaps } = trpc.portal.list.useQuery({ type: "gap", limit: 3 });
  const { data: conflicts } = trpc.portal.list.useQuery({ type: "conflict", limit: 3 });
  return <div className="space-y-8"><section className="relative overflow-hidden rounded-[2rem] bg-[#12362b] px-6 py-8 text-white md:px-10 md:py-12"><div className="absolute -right-24 -top-24 h-72 w-72 rounded-full bg-[#d9f99d]/15 blur-2xl" /><div className="relative max-w-3xl"><div className="mb-5 flex items-center gap-2 text-xs uppercase tracking-[0.22em] text-lime-200"><Leaf className="h-4 w-4" /> Evidence-first environmental operations</div><h2 className="text-4xl font-semibold tracking-tight md:text-6xl">A calmer way to find the next right step.</h2><p className="mt-5 max-w-2xl text-sm leading-7 text-emerald-50/75 md:text-base">Move from audited source material to operational clarity. Explore tasks, sites, compliance layers, documents, gaps and conflicts without losing the evidence trail.</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild className="bg-[#d9f99d] text-[#12362b] hover:bg-lime-200"><Link href="/start-here">Start orientation <ArrowRight className="ml-2 h-4 w-4" /></Link></Button><Button asChild variant="outline" className="border-white/25 bg-white/5 text-white hover:bg-white/10 hover:text-white"><Link href="/search">Search the evidence base <Search className="ml-2 h-4 w-4" /></Link></Button></div></div></section>
    <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[["domains","Knowledge domains",Layers3Icon],["tasks","Operational tasks",Target],["documents","Audited documents",FileSearch],["gaps","Open knowledge gaps",Flag]].map(([key,label,Icon]: any) => <Card key={key} className="border-emerald-950/10 bg-white"><CardContent className="flex items-end justify-between p-5"><div><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-3xl font-semibold text-slate-950">{stats?.[key] ?? "—"}</p></div><div className="rounded-xl bg-emerald-50 p-3 text-emerald-800"><Icon className="h-5 w-5" /></div></CardContent></Card>)}</section>
    <div className="grid gap-6 xl:grid-cols-[1.2fr_0.8fr]"><Card className="border-emerald-950/10 bg-white"><CardHeader><div className="flex items-center justify-between"><div><CardTitle className="text-xl">Begin with the work</CardTitle><CardDescription>Source-derived tasks that help orient a new Environment team member.</CardDescription></div><Button asChild variant="ghost" className="text-emerald-800"><Link href="/tasks">View all <ArrowRight className="ml-1 h-4 w-4" /></Link></Button></div></CardHeader><CardContent className="grid gap-3 md:grid-cols-2">{tasks?.map(item => <Link key={item.id} href={`/tasks?id=${item.id}`} className="rounded-2xl border border-emerald-950/10 p-4 transition hover:border-emerald-400 hover:bg-emerald-50/40"><StatusBadge status={item.status} /><p className="mt-3 text-sm font-semibold text-slate-900">{item.title}</p><p className="mt-2 line-clamp-2 text-xs leading-5 text-slate-500">{summaryFor(item)}</p></Link>)}</CardContent></Card><div className="space-y-6"><Card className="border-amber-200 bg-amber-50/70"><CardHeader className="pb-3"><CardTitle className="flex items-center gap-2 text-base text-amber-950"><AlertCircle className="h-4 w-4" /> Review before acting</CardTitle></CardHeader><CardContent className="space-y-3">{gaps?.slice(0, 2).map(item => <Link key={item.id} href="/knowledge-gaps" className="block text-sm leading-5 text-amber-900 hover:underline">{item.title}</Link>)}</CardContent></Card><Card className="border-rose-200 bg-rose-50/70"><CardHeader className="pb-3"><CardTitle className="flex items-center gap-2 text-base text-rose-950"><ShieldAlert className="h-4 w-4" /> Conflicts stay visible</CardTitle></CardHeader><CardContent className="space-y-3">{conflicts?.slice(0, 2).map(item => <Link key={item.id} href="/conflicts" className="block text-sm leading-5 text-rose-900 hover:underline">{item.title}</Link>)}</CardContent></Card></div></div>
  </div>;
}

const pathConfig: Record<string, { type: any; eyebrow: string; title: string; description: string }> = {
  "/topics": { type: "domain", eyebrow: "Knowledge architecture", title: "Topics & domains", description: "A structured map of the audited Environment knowledge base, organized around what people need to understand and do." },
  "/tasks": { type: "task", eyebrow: "Operational reference", title: "Tasks / How-To", description: "Find the purpose, steps, inputs, outputs, roles, required documents, regulations, gaps and citations for source-derived work." },
  "/workflows": { type: "workflow", eyebrow: "Process view", title: "Workflows", description: "Workflow shells assembled only from explicit source statements. Incomplete steps remain visibly marked for review." },
  "/sites": { type: "site_group", eyebrow: "Field reference", title: "Sites & facilities", description: "Source-derived site groups preserve original names, related activities, monitoring structures, record IDs and ambiguity notes. No universal asset master is assumed." },
  "/monitoring": { type: "monitoring", eyebrow: "Monitoring architecture", title: "Monitoring", description: "Six explicit monitoring structures connect activity, site, point, parameter, method, result, validation, report and source. Missing frequency, role and destination remain visible." },
  "/compliance": { type: "regulation", eyebrow: "Four distinct layers", title: "Compliance", description: "Keep regulations and standards, company requirements, permits and practices visually separate so applicability is not conflated." },
  "/documents": { type: "document", eyebrow: "Source library", title: "Documents", description: "The audited source inventory, with original filenames, document type, status, path and extraction state." },
  "/glossary": { type: "glossary", eyebrow: "Shared language", title: "Glossary", description: "Terms and expansions from the audited materials, with confirmation status and source references." },
  "/knowledge-gaps": { type: "gap", eyebrow: "Review queue", title: "Knowledge gaps", description: "Missing or unclear evidence is surfaced as a work item—not hidden behind a polished answer." },
  "/conflicts": { type: "conflict", eyebrow: "Verification", title: "Conflicts", description: "Source A and Source B remain visible side by side. The portal does not infer or fabricate a resolution." },
};

export function PortalRoute() {
  const [location, setLocation] = useLocation();
  const config = pathConfig[location.split("?")[0]] ?? pathConfig["/topics"];
  const params = new URLSearchParams(location.split("?")[1] ?? "");
  const detailId = params.get("id");
  const { data: detail, error: detailError } = trpc.portal.get.useQuery({ type: config.type, id: detailId ?? "" }, { enabled: Boolean(detailId) });
  const { data, isLoading, error } = trpc.portal.list.useQuery({ type: config.type, limit: config.type === "glossary" || config.type === "site" || config.type === "site_group" ? 60 : 30 });
  if (detailId && detailError) return <EmptyState title="Source record unavailable" description="The selected audited record could not be reached. Do not treat an empty response as evidence." />;
  if (detailId && detail) return <DetailView item={detail} onBack={() => setLocation(location.split("?")[0])} />;
  return <div><PageIntro eyebrow={config.eyebrow} title={config.title} description={config.description} action={config.type === "task" ? <Button asChild className="bg-[#12362b] hover:bg-emerald-900"><Link href="/take-over">Build a handover <ClipboardIcon className="ml-2 h-4 w-4" /></Link></Button> : undefined} /><div className="mb-6 flex items-center gap-2 rounded-2xl border border-emerald-950/10 bg-white p-4 text-xs text-slate-600"><Sparkles className="h-4 w-4 text-emerald-700" /><span>Every card carries an evidence status. Review the citation drawer before treating a record as operational guidance.</span></div>{isLoading ? <LoadingGrid /> : error ? <EmptyState title="Source library unavailable" description="The audited source index could not be reached. Try again later rather than relying on an unsupported answer." /> : data?.length ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.map(item => <EvidenceCard key={item.id} item={item} onOpen={() => setLocation(`${location.split("?")[0]}?id=${item.id}`)} />)}</div> : <EmptyState />}</div>;
}

function DetailView({ item, onBack }: { item: any; onBack: () => void }) {
  const data = item.data ?? {};
  const isConflict = item.status === "CONFLICTING";
  const isTask = item.type === "task";
  const isDocument = item.type === "document";
  const isSiteGroup = item.type === "site_group";
  return <div className="space-y-6"><Button variant="ghost" onClick={onBack} className="px-0 text-emerald-800">← Back to list</Button><div className="grid gap-6 xl:grid-cols-[1fr_320px]"><main className="space-y-6"><Card className="border-emerald-950/10 bg-white"><CardHeader><div className="flex flex-wrap items-center gap-2"><StatusBadge status={item.status} /><span className="text-xs text-slate-400">{item.type} · {item.id}</span></div><CardTitle className="mt-3 text-3xl leading-tight">{item.title}</CardTitle><CardDescription>{summaryFor(item)}</CardDescription></CardHeader><CardContent className="space-y-5">{isConflict ? <div className="grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-rose-200 bg-rose-50 p-5"><p className="text-xs font-semibold uppercase tracking-wider text-rose-800">Source A</p><p className="mt-3 text-sm leading-6 text-rose-950">{String(data.source_a_statement ?? "Statement not available")}</p><p className="mt-4 text-[11px] text-rose-700">{String(data.source_a ?? "Source locator unavailable")}</p></div><div className="rounded-2xl border border-rose-200 bg-rose-50 p-5"><p className="text-xs font-semibold uppercase tracking-wider text-rose-800">Source B</p><p className="mt-3 text-sm leading-6 text-rose-950">{String(data.source_b_statement ?? "Statement not available")}</p><p className="mt-4 text-[11px] text-rose-700">{String(data.source_b ?? "Source locator unavailable")}</p></div></div> : <div className="space-y-4">{isTask && <TaskFields data={data} />}{isDocument && <DocumentFields data={data} />}{isSiteGroup && <SiteGroupFields data={data} />}{!isTask && !isDocument && !isSiteGroup && <DataFields data={data} />}</div>}{item.status === "PARTIAL_EVIDENCE" && <div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-950"><strong>PARTIAL_EVIDENCE:</strong> Some fields are not present in the current source material. This record is a useful orientation, not a complete procedure.</div>}</CardContent></Card><CitationDrawer refs={item.refs} /></main><aside className="space-y-4"><TrustLayer status={item.status} /></aside></div></div>;
}

function TaskFields({ data }: { data: any }) { const { data: gaps } = trpc.portal.list.useQuery({ type: "gap", limit: 30 }); const taskRefs = new Set((data.source_document_ids ?? []).map(String)); const relatedGaps = (gaps ?? []).filter(gap => { const gapRefs = Array.isArray(gap.data?.source_document_ids) ? gap.data.source_document_ids : []; return gapRefs.some((id: unknown) => taskRefs.has(String(id))); }).slice(0, 5); const fields = [["Purpose",data.purpose],["Trigger",data.trigger],["Inputs",data.inputs],["Outputs",data.outputs],["Roles",data.responsible_role],["Required documents",data.required_documents],["Related regulations",data.related_regulations]]; return <div className="space-y-4"><div className="grid gap-4 md:grid-cols-2">{fields.map(([label,value]) => <div key={label} className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">{label}</p><p className="mt-2 text-sm leading-6 text-slate-700">{value ? String(value) : <span className="inline-flex items-center gap-1.5 text-amber-800"><AlertCircle className="h-4 w-4" /> PARTIAL_EVIDENCE — not found in current source material</span>}</p></div>)}</div><div className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">Step-by-step process</p>{Array.isArray(data.workflow_steps) && data.workflow_steps.length ? <ol className="mt-3 space-y-2">{data.workflow_steps.map((step: unknown, index: number) => <li key={index} className="flex gap-3 text-sm leading-6 text-slate-700"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-emerald-100 text-xs font-semibold text-emerald-800">{index + 1}</span><span>{String(step)}</span></li>)}</ol> : <p className="mt-2 text-sm text-amber-800">PARTIAL_EVIDENCE — step sequence not found in current source material.</p>}</div><div className="rounded-2xl border border-amber-200 bg-amber-50/60 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-amber-800">Linked knowledge gaps</p>{relatedGaps.length ? <div className="mt-3 space-y-2">{relatedGaps.map(gap => <Link key={gap.id} href="/knowledge-gaps" className="block text-sm leading-5 text-amber-950 hover:underline">{gap.title}</Link>)}</div> : <p className="mt-2 text-sm text-amber-900">PARTIAL_EVIDENCE — no linked gap record was found by shared source identifiers.</p>}</div></div>; }
function SiteGroupFields({ data }: { data: any }) { const { data: monitoring } = trpc.portal.list.useQuery({ type: "monitoring", limit: 10 }); const { data: parameters } = trpc.portal.list.useQuery({ type: "parameter", limit: 139 }); const refs = new Set((data.source_documents ?? []).map(String)); const relatedMonitoring = (monitoring ?? []).filter(item => item.refs.some(ref => refs.has(String(ref).split(" |")[0]))); const relatedParameters = (parameters ?? []).filter(item => { const sourceRefs = item.data?.source_document_ids ?? item.data?.source_references ?? []; return Array.isArray(sourceRefs) && sourceRefs.some((ref: unknown) => refs.has(String(ref).split(" |")[0])); }).slice(0, 12); return <div className="space-y-4"><div className="grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">Identity</p><p className="mt-2 text-lg font-semibold text-slate-950">{String(data.site_group_name ?? "Site group name not found")}</p><p className="mt-2 text-sm leading-6 text-slate-600">{String(data.grouping_rule ?? "Identity is source-text grouped and not a definitive asset master.")}</p></div><div className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">Facilities / source records</p><p className="mt-2 text-sm leading-6 text-slate-700">{Array.isArray(data.record_ids) && data.record_ids.length ? data.record_ids.join(", ") : "PARTIAL_EVIDENCE — facility records not found"}</p><p className="mt-2 text-xs text-slate-500">These IDs remain separate unless the audited evidence establishes identity.</p></div></div><div className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">Monitoring structures</p>{relatedMonitoring.length ? <div className="mt-3 grid gap-2 md:grid-cols-2">{relatedMonitoring.map(item=><Link key={item.id} href="/monitoring" className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-950">{item.title}<span className="mt-1 block"><StatusBadge status={item.status} /></span></Link>)}</div> : <p className="mt-2 text-sm text-amber-800">PARTIAL_EVIDENCE — no monitoring structure can be linked by shared source identifiers.</p>}</div><div className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">Related parameters</p>{relatedParameters.length ? <div className="mt-3 flex flex-wrap gap-2">{relatedParameters.map(item=><span key={item.id} className="rounded-full bg-slate-100 px-3 py-1.5 text-xs text-slate-700">{item.title}</span>)}</div> : <p className="mt-2 text-sm text-amber-800">PARTIAL_EVIDENCE — related parameter linkage not found in the current source set.</p>}</div></div>; }
function DocumentFields({ data }: { data: any }) { const fields = [["Original filename",data.filename],["Document type",data.document_type ?? data.file_type],["Status",data.status ?? data.confirmation_status ?? data.currency_status],["Source path",data.relative_path ?? data.path],["Effective date",data.effective_date ?? data.effectiveDate],["Version",data.version ?? data.revision],["Duplicate status",data.duplicate_status ?? data.duplicates],["Extraction state",data.extraction_status ?? (data.unprocessed ? "UNPROCESSED" : undefined)]]; return <div className="grid gap-4 md:grid-cols-2">{fields.map(([label,value]) => <div key={label} className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">{label}</p><p className="mt-2 text-sm leading-6 text-slate-700">{value ? String(value) : <span className="inline-flex items-center gap-1.5 text-amber-800"><AlertCircle className="h-4 w-4" /> PARTIAL_EVIDENCE — not found in current source material</span>}</p></div>)}</div>; }
function DataFields({ data }: { data: any }) { const entries = Object.entries(data).filter(([key,value]) => !["source_references","source_document_ids","document_ids"].includes(key) && value !== null && value !== undefined && value !== "").slice(0, 12); return <div className="grid gap-4 md:grid-cols-2">{entries.map(([key,value]) => <div key={key} className="rounded-2xl border border-emerald-950/10 p-4"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-700">{key.replaceAll("_"," ")}</p><p className="mt-2 line-clamp-6 text-sm leading-6 text-slate-700">{typeof value === "string" ? value : JSON.stringify(value)}</p></div>)}</div>; }

export function CompliancePage() { const { data } = trpc.portal.list.useQuery({ type: "regulation", limit: 60 }); const layerConfig = [{ title: "Regulations & standards", note: "External instruments and standards cited by the audited materials.", tone: "border-sky-200 bg-sky-50/70", items: data?.filter(item => !/permit|pkkprl|approval/i.test(item.title)).slice(0, 6) ?? [] }, { title: "Company requirements", note: "Only show when the source set states an internal requirement explicitly.", tone: "border-violet-200 bg-violet-50/70", items: data?.filter(item => /company|internal|sml|proper/i.test(item.title)).slice(0, 6) ?? [] }, { title: "Permits & approvals", note: "Permit and approval records remain distinct from general legal instruments.", tone: "border-amber-200 bg-amber-50/70", items: data?.filter(item => /permit|pkkprl|approval/i.test(item.title)).slice(0, 6) ?? [] }, { title: "Practices & guidance", note: "Operational practice is not automatically a legal obligation.", tone: "border-emerald-200 bg-emerald-50/70", items: [] }]; return <div><PageIntro eyebrow="Four distinct layers" title="Compliance" description="Keep regulations and standards, company requirements, permits and approvals, and practices or guidance structurally separate. Applicability and legal validity remain source-dependent." /><div className="grid gap-4 md:grid-cols-2">{layerConfig.map(layer => <Card key={layer.title} className={`${layer.tone} border`}><CardHeader><CardTitle className="text-base text-slate-950">{layer.title}</CardTitle><CardDescription className="text-slate-600">{layer.note}</CardDescription></CardHeader><CardContent className="space-y-2">{layer.items.length ? layer.items.map(item => <Link key={item.id} href={`/compliance?id=${item.id}`} className="block rounded-xl bg-white/70 p-3 text-sm font-medium text-slate-800 hover:bg-white">{item.title}<span className="mt-1 block"><StatusBadge status={item.status} /></span></Link>) : <div className="rounded-xl border border-dashed border-slate-300 bg-white/50 p-4 text-sm leading-6 text-slate-600">No consolidated record is assigned to this layer from the current seed. Review Documents and Knowledge Gaps rather than treating the layer as complete.</div>}</CardContent></Card>)}</div></div>; }

export function SearchPage() { const [query,setQuery] = useState(""); const [typeFilter,setTypeFilter] = useState<string | undefined>(); const [statusFilter,setStatusFilter] = useState<string | undefined>(); const { data, isFetching, error } = trpc.portal.search.useQuery({ query, type: typeFilter as any, status: statusFilter }, { enabled: query.trim().length > 1 }); const { data: suggestions } = trpc.portal.searchSuggestions.useQuery({ query }, { enabled: query.trim().length > 2 }); const { data: facets } = trpc.portal.searchFacets.useQuery({ query }, { enabled: query.trim().length > 1 }); return <div><PageIntro eyebrow="Cross-knowledge search" title="Search the evidence base" description="Search audited records with metadata facets, status filters, acronym expansion and light typo tolerance. Every result retains its evidence state and citations." /><div className="mb-3 grid max-w-4xl gap-2 md:grid-cols-[1fr_180px_180px_auto]"><div className="relative"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Try “water sampling”, “PKKPRL”, or “oil & grease”" className="h-11 border-emerald-950/15 bg-white pl-10" /></div><select value={typeFilter ?? ""} onChange={event=>setTypeFilter(event.target.value || undefined)} className="h-11 rounded-xl border border-emerald-950/15 bg-white px-3 text-sm"><option value="">All record types</option><option value="task">Tasks</option><option value="site_group">Site groups</option><option value="monitoring">Monitoring</option><option value="document">Documents</option><option value="regulation">Regulations</option><option value="parameter">Parameters</option><option value="glossary">Glossary</option><option value="gap">Gaps</option><option value="conflict">Conflicts</option></select><select value={statusFilter ?? ""} onChange={event=>setStatusFilter(event.target.value || undefined)} className="h-11 rounded-xl border border-emerald-950/15 bg-white px-3 text-sm"><option value="">All statuses</option><option value="SOURCE_SUPPORTED">Source supported</option><option value="PARTIAL_EVIDENCE">Partial evidence</option><option value="CONFLICTING">Conflicting</option><option value="REQUIRES_HUMAN_REVIEW">Human review</option></select><Button className="h-11 bg-[#12362b] hover:bg-emerald-900" disabled={!query.trim()}>Search</Button></div>{facets && <div className="mb-4 flex flex-wrap gap-2 text-[11px] text-slate-500"><span className="font-semibold uppercase tracking-[0.14em] text-emerald-800">Facets</span>{facets.types.map(([name,count])=><button key={name} onClick={()=>setTypeFilter(name)} className="rounded-full border border-emerald-950/10 bg-white px-2.5 py-1 hover:border-emerald-400 hover:text-emerald-800">{name.replaceAll("_"," ")} · {count}</button>)}{facets.statuses.slice(0,4).map(([name,count])=><button key={name} onClick={()=>setStatusFilter(name)} className="rounded-full border border-emerald-950/10 bg-white px-2.5 py-1 hover:border-emerald-400 hover:text-emerald-800">{name.replaceAll("_"," ")} · {count}</button>)}</div>}{suggestions?.length ? <div className="mb-6 flex flex-wrap gap-2">{suggestions.map(suggestion=><button key={suggestion} onClick={()=>setQuery(suggestion)} className="rounded-full border border-emerald-950/10 bg-white px-3 py-1.5 text-xs text-slate-600 hover:border-emerald-400 hover:text-emerald-800">{suggestion}</button>)}</div> : null}{query && <p className="mb-4 text-xs text-slate-500">{error ? "Source search is temporarily unavailable." : isFetching ? "Searching audited records…" : `${data?.length ?? 0} results with matching source text`}</p>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data?.map(item => <EvidenceCard key={`${item.type}-${item.id}`} item={item} />)}</div>{query && !isFetching && !error && !data?.length && <EmptyState title="No matching evidence" description="Information not found in the current Environment Knowledge Base." />}{error && <EmptyState title="Source search unavailable" description="The audited source index could not be reached. Try again later rather than relying on an unsupported answer." />}</div>; }

export function AIPage() { const [messages,setMessages] = useState<Message[]>([]); const ask = trpc.portal.aiAsk.useMutation(); const send = async (text: string) => { setMessages(prev => [...prev,{role:"user",content:text}]); const result = await ask.mutateAsync({ question:text }); const status = result.status; const citations = result.citations?.length ? `\n\n**Sources**\n${result.citations.map(c => `- ${c}`).join("\n")}` : ""; const conflict = result.conflicts?.length ? `\n\n**Verification required:** Source A and Source B remain unresolved.` : ""; setMessages(prev => [...prev,{role:"assistant",content:`**${statusMeta[status]?.label ?? status}**\n\n${result.answer}${citations}${conflict}`}]); }; return <div><PageIntro eyebrow="Source-grounded assistant" title="Ask the Environment Knowledge Base" description="Questions are classified, matched to audited evidence, and answered with visible status and citations. The assistant never resolves conflicts or guesses beyond the source set." /><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_300px]"><Card className="border-emerald-950/10 bg-white"><CardContent className="p-3 md:p-5"><AIChatBox messages={messages} onSendMessage={send} isLoading={ask.isPending} placeholder="Ask about a task, site, parameter, requirement, or source…" suggestedPrompts={["How do I perform environmental sampling?","Which requirements are conflicting?","Which document explains water monitoring?"]} /></CardContent></Card><Card className="h-fit border-emerald-950/10 bg-[#eaf5e8]"><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Sparkles className="h-4 w-4 text-emerald-700" /> Response policy</CardTitle></CardHeader><CardContent className="space-y-3 text-sm leading-6 text-emerald-950"><p>Company-specific answers require retrieved source evidence.</p><p>Partial evidence is labeled. Conflicts show Source A vs Source B.</p><p>If evidence is missing, the answer is: “Information not found in the current Environment Knowledge Base.”</p></CardContent></Card></div></div>; }

export function HandoverPage() { const { data: tasks } = trpc.portal.list.useQuery({ type:"task", limit:50 }); const { user } = useAuth(); const [selected,setSelected] = useState<string[]>([]); const [name,setName] = useState("Environment handover"); const [created,setCreated] = useState<number | null>(null); const create = trpc.portal.handoverCreate.useMutation(); const toggle=(id:string)=>setSelected(prev=>prev.includes(id)?prev.filter(item=>item!==id):[...prev,id]); if (created) return <HandoverWorkspace projectId={created} tasks={tasks ?? []} />; return <div><PageIntro eyebrow="Transition workspace" title="I’m Taking Over" description="Create a personal handover workspace from source-derived tasks. Open gaps and conflicts remain visible as review prompts." /><div className="grid gap-6 xl:grid-cols-[1fr_340px]"><Card className="border-emerald-950/10 bg-white"><CardHeader><CardTitle>Choose the work you are taking over</CardTitle><CardDescription>Selection is drawn from the audited task catalog.</CardDescription></CardHeader><CardContent className="space-y-3">{tasks?.map(item => <label key={item.id} className={`flex cursor-pointer items-start gap-3 rounded-2xl border p-4 transition ${selected.includes(item.id)?"border-emerald-500 bg-emerald-50":"border-emerald-950/10 hover:bg-emerald-50/40"}`}><input type="checkbox" checked={selected.includes(item.id)} onChange={()=>toggle(item.id)} className="mt-1 h-4 w-4 accent-emerald-700" /><div><p className="text-sm font-semibold text-slate-900">{item.title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{summaryFor(item)}</p></div></label>)}</CardContent></Card><Card className="h-fit border-emerald-950/10 bg-white"><CardHeader><CardTitle>Handover setup</CardTitle><CardDescription>Progress and notes are user-owned.</CardDescription></CardHeader><CardContent className="space-y-4"><Input value={name} onChange={e=>setName(e.target.value)} placeholder="Workspace name" /><div className="rounded-2xl bg-slate-50 p-4 text-sm text-slate-600"><span className="font-semibold text-slate-900">{selected.length}</span> tasks selected</div><Button className="w-full bg-[#12362b] hover:bg-emerald-900" disabled={!selected.length || create.isPending} onClick={async()=>{if(!user){startLogin();return;}const result=await create.mutateAsync({name,taskIds:selected});setCreated(result.projectId)}}>{user?"Create handover workspace":"Sign in to save workspace"}</Button><p className="text-xs leading-5 text-slate-500">You will be able to update status, add notes, and record missing information after creation.</p></CardContent></Card></div></div>; }

function HandoverWorkspace({ projectId, tasks }: { projectId: number; tasks: any[] }) { const utils = trpc.useUtils(); const { data, isLoading, error } = trpc.portal.handoverGet.useQuery({ projectId }); const update = trpc.portal.handoverItemUpdate.useMutation({ onSuccess: () => utils.portal.handoverGet.invalidate({ projectId }) }); const addNote = trpc.portal.handoverNote.useMutation({ onSuccess: () => { utils.portal.handoverGet.invalidate({ projectId }); setNote(""); } }); const editNote = trpc.portal.handoverNoteEdit.useMutation({ onSuccess: () => { utils.portal.handoverGet.invalidate({ projectId }); setEditingNote(null); } }); const deleteNote = trpc.portal.handoverNoteDelete.useMutation({ onSuccess: () => utils.portal.handoverGet.invalidate({ projectId }) }); const [note,setNote] = useState(""); const [editingNote,setEditingNote] = useState<number | null>(null); if (isLoading) return <LoadingGrid />; if (error || !data) return <EmptyState title="Handover workspace unavailable" description="The workspace could not be loaded. Sign in again or ask an administrator to verify access." />; const done = data.items.filter(item=>item.itemStatus === "DONE").length; return <div><PageIntro eyebrow="User-owned handover" title={data.project.name} description="Track what is complete, what is in progress, and what still requires evidence or human review." action={<div className="rounded-2xl bg-emerald-50 px-4 py-3 text-sm text-emerald-900"><strong>{done}/{data.items.length}</strong> tasks complete</div>} /><div className="grid gap-6 xl:grid-cols-[1fr_340px]"><div className="space-y-4">{data.items.map(item => { const task = tasks.find(candidate=>candidate.id===item.taskExternalId); const taskData = task?.data ?? {}; const missing = ["purpose","inputs","outputs","responsible_role"].filter(key=>!taskData[key]); return <Card key={item.id} className="border-emerald-950/10 bg-white"><CardContent className="space-y-4 p-5"><div className="flex flex-col justify-between gap-3 md:flex-row md:items-start"><div><p className="text-xs uppercase tracking-wider text-emerald-700">Task {item.orderIndex + 1}</p><h3 className="mt-1 font-semibold text-slate-950">{task?.title ?? item.taskExternalId}</h3></div><select value={item.itemStatus} onChange={e=>update.mutate({projectId,itemId:item.id,itemStatus:e.target.value as "NOT_STARTED"|"IN_PROGRESS"|"BLOCKED"|"DONE",note:item.note ?? undefined})} className="rounded-xl border border-emerald-950/15 bg-white px-3 py-2 text-xs"><option value="NOT_STARTED">Not started</option><option value="IN_PROGRESS">In progress</option><option value="BLOCKED">Blocked</option><option value="DONE">Done</option></select></div><p className="text-sm leading-6 text-slate-600">{task ? summaryFor(task) : "Source task record unavailable in the current seed."}</p>{missing.length ? <div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs leading-5 text-amber-950"><strong>Missing-information prompt:</strong> Review {missing.join(", ")} in the source record before marking complete.</div> : null}<Textarea defaultValue={item.note ?? ""} placeholder="Add a handover note for this task…" onBlur={e=>update.mutate({projectId,itemId:item.id,itemStatus:item.itemStatus as "NOT_STARTED"|"IN_PROGRESS"|"BLOCKED"|"DONE",note:e.target.value})} /></CardContent></Card>; })}</div><aside className="space-y-4"><Card className="border-emerald-950/10 bg-white"><CardHeader><CardTitle className="text-base">Handover notes</CardTitle><CardDescription>Keep context separate from source evidence.</CardDescription></CardHeader><CardContent className="space-y-3"><Textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Add a workspace note…" /><Button className="w-full bg-[#12362b] hover:bg-emerald-900" disabled={!note.trim() || addNote.isPending} onClick={()=>addNote.mutate({projectId,body:note})}>Save note</Button>{data.notes.map(entry=><div key={entry.id} className="rounded-xl bg-slate-50 p-3 text-sm leading-5 text-slate-700">{editingNote === entry.id ? <div className="space-y-2"><Textarea defaultValue={entry.body} onBlur={e=>editNote.mutate({projectId,noteId:entry.id,body:e.target.value})} /><p className="text-[11px] text-slate-500">Editing saves when focus leaves the field.</p></div> : <><p>{entry.body}</p><div className="mt-2 flex gap-2"><Button variant="ghost" size="sm" className="h-7 px-2 text-xs text-slate-600" onClick={()=>setEditingNote(entry.id)}><Pencil className="mr-1 h-3 w-3" />Edit</Button><Button variant="ghost" size="sm" className="h-7 px-2 text-xs text-rose-700" onClick={()=>deleteNote.mutate({projectId,noteId:entry.id})}><Trash2 className="mr-1 h-3 w-3" />Delete</Button></div></>}</div>)}</CardContent></Card><TrustLayer status="REQUIRES_HUMAN_REVIEW" /></aside></div></div>; }

function LoadingGrid(){return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{Array.from({length:6}).map((_,i)=><div key={i} className="h-52 animate-pulse rounded-2xl bg-emerald-950/5" />)}</div>}
function EmptyState({title="No records in this view",description="The current source set does not contain a published record for this view."}:{title?:string;description?:string}){return <div className="rounded-3xl border border-dashed border-emerald-950/20 bg-white p-10 text-center"><CircleHelp className="mx-auto h-8 w-8 text-emerald-700" /><p className="mt-3 font-semibold text-slate-900">{title}</p><p className="mt-2 text-sm text-slate-500">{description}</p></div>}
function Layers3Icon(props:any){return <Layers3 {...props}/>}
function ClipboardIcon(props:any){return <ClipboardCheck {...props}/>}
